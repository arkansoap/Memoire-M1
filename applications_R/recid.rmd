---
title: "recidivism"
author: "thibault FUCHEZ"
date: "13/05/2021"
output: html_document
editor_options: 
  chunk_output_type: console
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
library(readr)
```

```{r}
data <- read_csv("C:/Users/arkan/OneDrive/Bureau/recidivism-COMPAS.csv")
```

```{r}
str(data)
```

```{r}
data<- data[,c("id","sex","age_cat","race","juv_fel_count","juv_misd_count","juv_other_count", "priors_count", "c_jail_in","c_jail_out","c_charge_degree","c_charge_desc","is_recid")]
```

Voc: 

battery : coups et blessure

degree charge : F fellony (crime et délit) / M midemeanor (infraction)

priors : antécédents

```{r}
str(data)
```

```{r}
retype <- function(data)
  {
  for (i in col(data))
    if (class(i))is.numeric:data[,i] <- as.factor(data[,i])
  }
```

```{r}
retype(data)
```


```{r}
plot(data$is_recid)
```


